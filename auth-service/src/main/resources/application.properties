# Nombre del servicio (Así lo buscará el Gateway)
spring.application.name=auth-service

# Puerto propio
server.port=8081

# Conexión con la "Guía Telefónica" (Eureka)
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/