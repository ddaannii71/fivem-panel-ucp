spring.application.name=mgmt-service
server.port=8083

# --- EUREKA ---
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true
eureka.instance.ip-address=127.0.0.1
eureka.instance.hostname=localhost

# --- FXSERVER nativo (status) ---
fxserver.ip=127.0.0.1
fxserver.port=30120

# --- LOGGING ---
logging.level.com.fivem.panel.mgmt_service=INFO

# --- TXADMIN API (kick, ban, etc.) ---
txadmin.base-url=http://localhost:40120
txadmin.username=test
txadmin.password=testtest
